<?xml version="1.0" encoding="utf-8"?>
<!-- The one action on a screen. Accent at rest, so it is findable by touch with no focus
     at all; a light ring when focused, so a gamepad cursor sitting on it is unmistakable. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/accent_deep" />
            <stroke android:width="3dp" android:color="@color/text_primary" />
            <corners android:radius="12dp" />
        </shape>
    </item>
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/accent" />
            <stroke android:width="3dp" android:color="@color/text_primary" />
            <corners android:radius="12dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/accent" />
            <stroke android:width="3dp" android:color="@android:color/transparent" />
            <corners android:radius="12dp" />
        </shape>
    </item>
</selector>
